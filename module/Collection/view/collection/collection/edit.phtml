<?php
$backUrl = $this->url('collection-details', array('id' => $collection->getId(), 'section' => $section));

echo $this->partial('formCollection', array(
    'header' => $this->translate("Edit Collection"),
    'collection' => $collection,
    'backUrl' => $backUrl,
    'clients' => $clients,
    'section' => $section,
));