<?php
$backUrl = $this->url('collection', array('action' => 'details', 'id' => $collection->getId()));

echo $this->partial('formCollection', array(
    'header' => $this->translate("Edit Collection"),
    'collection' => $collection,
    'client' => $client,
    'backUrl' => $backUrl,
    'section' => $section,
));