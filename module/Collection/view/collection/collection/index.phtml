<?php
use Collection\Model\Collection;
use Collection\Enum\CollectionEnum;
use Application\Enum\SectionEnum;

$section = $this->sectionName();

echo ($section == SectionEnum::COLLECTION_INDEX) ? "<h1>" : "<h2>";
    echo $this->translate('Collections List');
echo ($section == SectionEnum::COLLECTION_INDEX) ? "</h1>" : "</h2>";
?>
<p>
    <a href="<?php echo $this->url('collection-add', array(
                                                        'id' => CollectionEnum::NEW_COLLECTION,
                                                        'clientId' => (isset($clientId)) ? $clientId : false,
                                                        'section' => $section,
        )
    );?>">
        <?php echo $this->translate("Add new collection"); ?>
    </a>
</p>

<table class="table">
    <th>
        <?php echo $this->translate("Reception Date"); ?>
    </th>
    <?php
        if($section == SectionEnum::COLLECTION_INDEX) {
    ?>
        <th><?php echo $this->translate("Client"); ?></th>
    <?php
        }
    ?>
    <th>
        <?php echo $this->translate("Bows"); ?>
    </th>
    <th>
        <?php echo $this->translate("Options"); ?>
    </th>
    <?php
        /**  @var  $collection Collection*/
         foreach ($collections as $collection) :
    ?>
        <tr id="collection-<?php echo $collection->getId() ?>">
            <td>
                <?php echo $collection->getReceptionTime() ?>
            </td>
            <?php
            if($section == SectionEnum::COLLECTION_INDEX) {
                ?>
                <td>
                    <a href="<?php echo $this->url('client-details', array('id' => $collection->getOwnerId()));?>">
                        <?php echo $collection->getOwnerName() ?>
                    </a>
                </td>
            <?php
            }
            ?>
            <td>
                <?php echo $collection->countBows() ?>
            </td>
            <td>
                <a href="<?php echo $this->url('collection-details',  array('id' => $collection->getId(),'section' => $section)); ?>"><?php
                    echo $this->translate("Details")
                    ?></a>
                <a href="<?php echo $this->url('collection-edit', array('id' => $collection->getId(), 'section' => $section)); ?>"><?php
                    echo $this->translate("Edit")
                    ?></a>
                <div class="collection-delete" data-id="<?php echo $collection->getId() ?>" ><?php
                    echo $this->translate("Delete")
                    ?><div>
            </td>
        </tr>
    <?php endforeach; ?>
</table>
<?php
    $this->placeholder('scripts')->append("
        BowsManager.collection.del();
    ");
?>